services:
  clamav:
    image: clamav/clamav:stable
    container_name: meditrade-clamav
    restart: unless-stopped
    ports:
      - "3310:3310"
    volumes:
      - clamav-db:/var/lib/clamav

volumes:
  clamav-db:
